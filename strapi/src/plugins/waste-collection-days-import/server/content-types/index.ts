// Copying message from Marianum project:
// Unfortunately, the content types must be created in the main Strapi codebase as I was able to set roles for their API routes if created here.
export default {}
