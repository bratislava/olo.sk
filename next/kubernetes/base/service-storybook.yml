apiVersion: v1
kind: Service
metadata:
  name: ${BUILD_REPOSITORY_NAME}-storybook
  namespace: ${NAMESPACE}
  labels:
    service: storybook
spec:
  ports:
    - protocol: TCP
      port: 3000
      targetPort: storybook
  selector:
    service: app
